version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    # Optionally add:
    # open-pull-requests-limit: 5
    # commit-message:
    #   prefix: "chore(deps): "
    #   include: scope
